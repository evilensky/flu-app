<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Fluomics" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body class="background-dark wysihtml5-supported" style="zoom: 1;">

    <div class="logo">
      <h4><%= link_to 'Fluomics', root_url %></h4>
    </div>

    <nav id="sidebar" class="sidebar nav-collapse collapse" role="navigation">
      <ul id="side-nav" class="side-nav">
        <li class="<%= current_page?(root_path) ? 'active' : 'panel' %>">
          <%= link_to root_path do %>
            <span class="name">Home</span>
          <% end %>
        </li>
        <li class="<%= current_page?(links_path) ? 'active' : 'panel' %>">
          <%= link_to links_path do %>
            <span class="name">Links</span>
          <% end %>
        </li>
        <li class="<%= current_page?(about_us_path) ? 'active' : 'panel' %>">
          <%= link_to about_us_path do %>
            <span class="name">About Us</span>
          <% end %>
        </li>
      </ul>
    </nav>

    <div class="wrap">
      <header class="page-header">
        <div class="navbar">
          <ul class="nav navbar-nav navbar-right pull-right">
            <li class="visible-xs">
              <%= link_to('#', class: 'btn-navbar collapsed', :'data-toggle' => 'collapse', :'data-target' => '.sidebar') do %>
                <i class="icon-reorder"></i>
              <% end %>
            </li>
          </ul>
        </div>
      </header>
      <div class="content container">
        <div class="row">
          <div class="col-md-7">
            <% flash.each do |type, message| %>
              <div class="alert alert-<%= type %>"><%= message %></div>
            <% end %>

            <%= yield %>
          </div>
          <div class="col-md-3 offset-2">
            <%= image_tag 'NU_Logo.png' %>
            <%= link_to image_url('flu.png') do %>
              <%= image_tag('flu-thumb.png') %>
            <% end %>
          </div>
        </div><!--/row-->

      </div> <!-- /container -->
    </div>

  </body>
</html>
